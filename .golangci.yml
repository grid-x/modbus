version: "2"
linters:
  default: none
  enable:
    - staticcheck
    - gosec
    - govet
    - revive
  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
